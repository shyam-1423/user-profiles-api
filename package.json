{
    "name": "json-server-api",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch fakeUsers.json --host 0.0.0.0 --port $PORT"
    },
    "dependencies": {
        "json-server": "^0.17.3"
    }
}